name: Build Examples on PR

on:
  pull_request:

jobs:
  examples-build:
    runs-on: ubuntu-latest
    outputs:
      is_speakeasy_pr: ${{ steps.check-pr.outputs.is_speakeasy_pr }}
      target_branch: ${{ steps.check-pr.outputs.target_branch }}
    
    steps:
    - name: Check PR source and target
      id: check-pr
      run: |
        echo "Head ref: ${{ github.head_ref }}"
        echo "Base ref: ${{ github.base_ref }}"
        
        # Check if this is a Speakeasy-generated PR
        if [[ "${{ github.head_ref }}" == speakeasy-sdk-regen* ]]; then
          echo "is_speakeasy_pr=true" >> "$GITHUB_OUTPUT"
        else
          echo "is_speakeasy_pr=false" >> "$GITHUB_OUTPUT"
        fi
        
        echo "target_branch=${{ github.base_ref }}" >> "$GITHUB_OUTPUT"

    - name: Checkout code
      uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
      
    - name: Set up Go
      uses: actions/setup-go@44694675825211faa026b3c33043df3e48a5fa00
      with:
        go-version: '1.23'
        
    - name: Cache Go modules
      uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-1.23-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-1.23-
          ${{ runner.os }}-go-
          
    - name: Prepare main module
      run: |
        go mod tidy
        go mod download
        
    - name: Prepare examples module
      run: |
        cd examples
        go mod tidy
        go mod download
        
    - name: Build standalone examples (ending with -auth.go)
      run: |
        cd examples
        echo "🔍 Building standalone examples (ending with -auth.go)..."
        
        standalone_examples=$(find . -name "example-*-auth.go" -type f | sort)
        
        if [ -n "$standalone_examples" ]; then
          for example_file in $standalone_examples; do
            example_name=$(basename "$example_file" .go)
            echo "🔨 Building $example_name (standalone)..."
            go build -v "$example_file"
            echo "✅ $example_name built successfully"
            echo ""
          done
        else
          echo "📭 No standalone examples found"
        fi
        
    - name: Build examples with auth.go dependency
      run: |
        cd examples
        echo "🔍 Building examples that require auth.go..."
        
        # Find all example-*.go files that don't end with -auth.go
        dependency_examples=$(find . -name "example-*.go" -type f ! -name "*-auth.go" | sort)
        
        if [ -n "$dependency_examples" ]; then
          for example_file in $dependency_examples; do
            example_name=$(basename "$example_file" .go)
            echo "🔨 Building $example_name (with auth.go)..."
            go build -v "$example_file" auth.go
            echo "✅ $example_name built successfully"
            echo ""
          done
        else
          echo "📭 No examples requiring auth.go found"
        fi

  auto-merge:
    name: Auto-merge Speakeasy PR (dev/stage only)
    needs: examples-build
    runs-on: ubuntu-latest
    # Only run for Speakeasy PRs targeting dev or stage branches
    if: |
      needs.examples-build.outputs.is_speakeasy_pr == 'true'
      #  &&
      # (needs.examples-build.outputs.target_branch == 'dev' || needs.examples-build.outputs.target_branch == 'stage')
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Auto-merge PR
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "Auto-merging Speakeasy PR #${{ github.event.pull_request.number }} targeting ${{ needs.examples-build.outputs.target_branch }}..."
          
          # gh pr merge ${{ github.event.pull_request.number }} \
          #   --repo "${{ github.repository }}" \
          #   --squash \
          #   --delete-branch \
          #   --auto
          
          echo "✅ Auto-merge enabled for PR #${{ github.event.pull_request.number }}"
