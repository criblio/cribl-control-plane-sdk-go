# ScheduleTypeEnum6

Select a schedule type; either an interval (in seconds) or a cron-style schedule.


## Values

| Name                            | Value                           |
| ------------------------------- | ------------------------------- |
| `ScheduleTypeEnum6Interval`     | interval                        |
| `ScheduleTypeEnum6CronSchedule` | cronSchedule                    |