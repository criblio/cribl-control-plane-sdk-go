# EndpointDynatraceHTTP

## Example Usage

```go
import (
	"github.com/criblio/cribl-control-plane-sdk-go/models/components"
)

value := components.EndpointDynatraceHTTPCloud

// Open enum: custom values can be created with a direct type cast
custom := components.EndpointDynatraceHTTP("custom_value")
```


## Values

| Name                              | Value                             |
| --------------------------------- | --------------------------------- |
| `EndpointDynatraceHTTPCloud`      | cloud                             |
| `EndpointDynatraceHTTPActiveGate` | activeGate                        |
| `EndpointDynatraceHTTPManual`     | manual                            |